---
const { ttl, mt } = Astro.props
---

<div class=`flex justify-center mb-[32px] ${mt} relative z-[30]`>
    <h2 class="h2 font-[700] text-[28px] w-full text-center pb-[.4em] relative bg-[#fff100] max-w-[328px]">{ ttl }</h2>
</div>

<style>
    .h2::before {
        position: absolute;
        top: -6px;
        left: -7px;
        width: 100%;
        height: 100%;
        content: '';
        border: 4px solid #000;
        z-index: 10;
    }
</style>